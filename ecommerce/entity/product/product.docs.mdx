---
description: 'A Product data structure component.'
labels: ['data', 'mock']
---

import { Product } from './product';
import { mockProductList } from './product.mocks';

## Model for data for products

An array of mock data to show some products according to the data structure. This data strucuture can be used by all stores to ensure the correct data is being passed and can also be extended incase new types need to be added.

### Component usage

```js
const list = mockProductList;
```
