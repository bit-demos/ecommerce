---
description: 'A Price component with currency'
labels: ['currency', price]
---

import { Price } from './price';

## Price component

A div that renders a currency and uses a prop to pass in the number

### Component usage

```js
<Price price={120} />
```

### Using props to customize the price

Modify the number to see it change live:

```js live
<Price price={120} />
```
